-- AlterEnum
ALTER TYPE "JobStatus" ADD VALUE IF NOT EXISTS 'CANCELED';
ALTER TYPE "JobStatus" ADD VALUE IF NOT EXISTS 'TIMEOUT';

-- AlterTable
ALTER TABLE "Job"
  ADD COLUMN IF NOT EXISTS "startedAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "finishedAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "timeoutAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "cancelReason" TEXT,
  ADD COLUMN IF NOT EXISTS "runnerHandle" TEXT;
