{
  "name": "worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "rm -rf dist && tsc -p tsconfig.build.json && mkdir -p dist/sandbox && cp sandbox/network_blocker.cjs dist/sandbox/",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests\""
  },
  "dependencies": {
    "@gauntlet/core": "workspace:*",
    "@prisma/client": "^6.1.0",
    "adm-zip": "^0.5.10",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.5"
  }
}
